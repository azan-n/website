---
interface Props {
    heading: string;
    description?: string;
}

const { heading, description } = Astro.props;
---

<section class="flex flex-col">
    <h1 class="mb-0 pb-0">{heading}</h1>
    {description && <p class="text-xl font-light">{description}</p>}
</section>
